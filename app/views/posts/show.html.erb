<h1>投稿詳細ページ</h1>
<div class="container">
  <h2><%= @post.title %></h2>
  <p>
    <img src= "<%= "/post_images/#{@post.image_name}" %>">
  </p>
  <p>
    <%= @post.content %>
  </p>
  <%= link_to("編集","/posts/#{@post.id}/edit") %>
</div>
